import { AdminApp } from '../../src/admin/AdminApp'

/** /admin — client-side admin app (sections via #/content, #/media, #/analytics, #/comments). */
export default function AdminPage() {
  return <AdminApp />
}
